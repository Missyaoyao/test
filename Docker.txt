Docker 是一个开源的应用容器引擎，让开发者可以将应用程序与该程序的依赖，打包在一个文件里面。
运行这个文件，就会生成一个虚拟容器。
程序在这个虚拟容器里运行，就好像在真实的物理机上运行一样。有了 Docker，就不用担心环境问题。
                                                                    ———— from 阮一峰'博客
